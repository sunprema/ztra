Package docstring only — a one-paragraph map of the core pipeline (world → protocol → pir → compiler → lower → backend), kept as the first thing a reader sees on `import ztra`. No code, no logic to narrate; it exists so the module tree's own docstring matches the pipeline order described in ARCHITECTURE.md.
